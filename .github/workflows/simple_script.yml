#------------------------------------------------------------------
# Запуск простого bash-скрипта для AWS EC2
#  
#------------------------------------------------------------------

name: Simple-script
#env:
#  VARIABLE 1       : "lt-04b30e2dc9209f94b"
#  VARIABLE 2       : "1"
  
# Запускаем по событию. 
on:
  # Ручками в Actions вкладке
  workflow_dispatch:
  
# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  
  # Задача: Запуск серверов
  Runnig script:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest
    
    # Используем настроенную среду
    environment:
      name: My_AWS
      
    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      # Аутентификация
      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id      : ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key  : ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region             : "eu-central-1"
          
      # Запускаем bash скрипт, пытаясь передать параметры
      - name: Executing bash-script
        run: sh ./instances.sh # ${{ env.VARIABLE1 }} ${{ env.VARIABLE2 }}
    
#EOF    
#""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
